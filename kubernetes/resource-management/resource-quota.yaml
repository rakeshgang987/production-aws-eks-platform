apiVersion: v1
kind: ResourceQuota

metadata:
  name: production-eks-platform-quota
  namespace: production-eks-platform

spec:
  hard:
    requests.cpu: "4"
    requests.memory: "4Gi"

    limits.cpu: "8"
    limits.memory: "8Gi"

    pods: "20"