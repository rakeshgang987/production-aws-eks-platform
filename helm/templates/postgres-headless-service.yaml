apiVersion: v1
kind: Service

metadata:
  name: postgres-headless
  namespace: {{ .Release.Namespace }}
  labels:
    app: postgres

spec:
  clusterIP: None

  selector:
    app: postgres

  ports:
    - protocol: TCP
      port: {{ .Values.postgres.service.port }}
      targetPort: 5432
